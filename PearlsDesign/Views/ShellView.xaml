<Window x:Class="PearlsDesign.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PearlsDesign.Views"
        mc:Ignorable="d" 
        FontSize="18"
        Title="PearlDesign" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="PearlTemplate">
            <Rectangle Fill="{Binding Pearls.Color}" Height="20" Width="20" />
        </DataTemplate>
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="FirstName" Grid.Row="0" Grid.Column="0"></TextBlock>

        <!--<ContentControl Content="{Binding PearlGrid}" Grid.Row="2" Grid.Column="2">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <Rectangle Fill="{Binding Pearls[0].Color}" Height="{Binding PearlBeadSize}" Width="{Binding PearlBeadSize}" />
                    </WrapPanel>
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>-->
        <!--ContentTemplate="{StaticResource PearlTemplate}"-->
        
        
        <!--<ContentControl Content="{Binding PearlGrid}" Grid.Row="2" Grid.Column="2">
            <ContentControl.ContentTemplate>
                <DataTemplate>-->
                    <ItemsControl ItemsSource="{Binding Pearls}" Background="Beige" Grid.Row="2" Grid.Column="2">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <!--<WrapPanel>-->
                                <Rectangle Fill="{Binding Color}" Height="20" Width="20" />
                                <!--</WrapPanel>-->
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                <!--</DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>-->
    </Grid>
</Window>
