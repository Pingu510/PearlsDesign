<Window x:Class="PearlsDesign.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xceedtk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:local="clr-namespace:PearlsDesign.Views"
        xmlns:base="clr-namespace:PearlsDesign"
        mc:Ignorable="d" 
        Title="PearlDesign"
        MinHeight="200" MinWidth="400"
        Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="PearlTemplate">
            <Rectangle Fill="{Binding Pearls.Color}" Height="20" Width="20" />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <Label Background="{Binding ColorOne}">Left</Label>
            <xceedtk:ColorPicker Name="ClrPcker_Left" DisplayColorAndName="True" cal:Message.Attach="[Event SelectedColorChanged] = [Action ClrPcker_SelectedColorChanged($source, $eventArgs, 'ColorOne')]"></xceedtk:ColorPicker>
            <Label Background="{Binding ColorTwo}">Right</Label>
            <xceedtk:ColorPicker Name="ClrPcker_Right" DisplayColorAndName="True" cal:Message.Attach="[Event SelectedColorChanged] = [Action ClrPcker_SelectedColorChanged($source, $eventArgs, 'ColorTwo')]"></xceedtk:ColorPicker>
            <Button x:Name="OpenSettingsPage">Settings</Button>
            <Button x:Name="GenerateNewGrid">Generate new Grid</Button>
        </StackPanel>

        <ContentControl Name="content" Content="{Binding PearlGrid}" Grid.Row="2" Grid.Column="2" >
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <!--Scrollabillity-->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <ItemsControl Name="itemC" ItemsSource="{Binding Pearls}"  Width="{Binding TotalGridWidth}" Height="{Binding TotalGridHeight}">
                            <ItemsControl.Resources>
                                <base:BindingProxy x:Key="proxy" Data="{Binding}" />
                            </ItemsControl.Resources>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Uid="{Binding Id}" 
                                            cal:Message.Attach="[Event PreviewMouseLeftButtonDown] = [Action Pearl_LeftClick($source)]; [Event PreviewMouseRightButtonDown] = [Action Pearl_RightClick($source)]"
                                            Height="{Binding Data.PearlBeadSize, Source={StaticResource proxy}}" 
                                            Width="{Binding Data.PearlBeadSize, Source={StaticResource proxy}}" 
                                            Background="{Binding FillColor}" BorderBrush="Black" BorderThickness="0.25">
                                        <!--<Button.Template>
                                            <ControlTemplate>
                                                <Rectangle Fill="{Binding Color}" Height="{Binding Data.PearlBeadSize, Source={StaticResource proxy}}" Width="{Binding Data.PearlBeadSize, Source={StaticResource proxy}}" Stroke="Black" StrokeThickness="0.25" />
                                            </ControlTemplate>
                                        </Button.Template>-->
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>
    </Grid>
</Window>